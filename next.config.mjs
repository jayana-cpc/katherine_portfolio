/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['assets.aceternity.com', 'upload.wikimedia.org', 'www.aavso.org', 'c02.purpledshub.com', 'i.imgur.com'],
  },
};

export default nextConfig;
